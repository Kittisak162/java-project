# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

#Get
GET     /index                      controllers.IndexController.index()

GET     /Car                        controllers.IndexController.tempshow(title:String,s:Int,l:Int,h:Int)

GET     /Cars                       controllers.IndexController.show(title:String)

GET     /about                      controllers.IndexController.about()

